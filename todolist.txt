Pins to assign:
  MAX31865:
    ? Set correct EXTI Prio for DR pin!
  LCD:
    ? Check about the functions and necessity of these pins again
      LCD_UPDATE was just used for profiling LCD update timing
  Touch:
    ? Recheck PEN/INT necessity, pullups and opendrain for reset?
      Only INT is used
  Stove:
    ? Port as a FreeRTOS task
  Mixer:
    ? Add mixer interface
Instrumentation:
  ? Add SYSVIEW instrumentation
General:
  ? Add 100 nF to reset line (maybe a pull-up, as well)
  ? LCD init is still not stable